//stuff to go here for barchart project

//some function to get info from html DOM or jQuerry
var getChart = function(){
  //initialize vars, data is array, options is just holder of properties, element is the DOM/jQuerry to render to
  var data, options, element;

  //create and return the thing to the site?
  return makeChart(data, options, element);
}

//some function to parse info and push chart (?)
var makeChart = function(data, options, element){

  //return the chart or data needed for chart
}